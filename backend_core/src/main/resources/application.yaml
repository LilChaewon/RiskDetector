spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/riskdetector
    username: risk_user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8080